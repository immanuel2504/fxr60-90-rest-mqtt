# `/cloud/region`

- **GET** - Get current region
- **PUT** - Set region (`setRegion`)

## Reviewed PUT

| File | Example name | Summary |
|---|---|---|
| `PUT/region.json` | `region` | USA FCC Part 15.247 |
| `PUT/success.json` | `success` | Empty string on success |

## NEED LIVE TEST

Confirm values against `GET /cloud/supportedRegionList` / `supportedStandardList` on the target reader.
