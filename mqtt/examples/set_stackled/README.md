# `set_stackled`

FXR60 only — stack LED (stack light). Not applicable to FXR90 (use `set_appled`).

Stable `command_id`: `req-set-stackled`

Supported colors: `red`, `amber`, `green`, `blue`, `off`  
Supported brightness: `low`, `med`, `high` (defaults to `low`)
