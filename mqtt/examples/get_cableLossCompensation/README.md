# `get_cableLossCompensation`

REST: `GET /cloud/cableLossCompensation` → `cloud-cablelosscompensation/`

Stable `command_id`: `req-get-cableLossCompensation`

| File | Direction | Example | REST source | Summary |
|---|---|---|---|---|
| `request/default.json` | request | `default` | `—` | Empty get_cableLossCompensation request |
| `response/configured_cable_loss.json` | response | `configured_cable_loss` | `cloud-cablelosscompensation/GET/configured_cable_loss.json` | Configured cable loss |
