# `get_certs`

REST: `GET /cloud/certificates` → `cloud-certificates/`

Stable `command_id`: `req-get-certs`

| File | Direction | Example | REST source | Summary |
|---|---|---|---|---|
| `request/default.json` | request | `default` | `—` | Empty get_certs request |
| `response/installed.json` | response | `installed` | `cloud-certificates/GET/installed.json` | Server and client certificates |
